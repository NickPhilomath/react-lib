// export const handleInputChange = ({ currentTarget: input }) => {
//   const newLog = { ...log };
//   if (input.type == "checkbox") {
//     newLog[input.name] = !newLog[input.name];
//   } else {
//     newLog[input.name] = input.value;
//   }
//   setLog(newLog);
// };
